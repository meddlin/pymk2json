# pymk2json - Python Markdown To JSON

An incredibly niche tool to convert Markdown tables to JSON objects.

## Testing

Built with Python's standard `unittest`.

```bash
cd pymk2json
python -m unittest tests.test_main
```